import { DiscordChannel } from "discordeno/types";
import { Client } from "../Client.js";

export class TextVoiceChannel {
    constructor(data: DiscordChannel, client: Client) {
        
    }
}

export default TextVoiceChannel;