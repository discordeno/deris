import { DiscordInvite } from "discordeno/types";
import Client from "../Client.js";

export class Invite {
    constructor(data: DiscordInvite, client: Client) {
        
    }
}

export default Invite;